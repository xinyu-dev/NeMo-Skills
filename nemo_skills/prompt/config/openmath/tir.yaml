system: ""

user: |-
  Solve the following math problem, integrating natural language reasoning with Python code executions.
  You may perform up to {total_code_executions} Python code calls to assist your reasoning. When using python code, you must use the following format:
  ```python
  # Your Python code here
  ```
  and the output of the code will be displayed in the following format:
  ```output
  # Code execution output will appear here
  ```

  Make sure to put the answer (and only answer) inside \boxed{{}}.

  {problem}